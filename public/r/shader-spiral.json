{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "shader-spiral",
  "title": "Shader Spiral",
  "description": "Rotating spiral arms radiating from the center.",
  "dependencies": [
    "@editframe/react",
    "@paper-design/shaders-react"
  ],
  "files": [
    {
      "path": "registry/bases/editframe/components/shader-spiral/index.tsx",
      "content": "\"use client\";\n\nimport { Timegroup, useTimingInfo } from \"@editframe/react\";\nimport { Spiral } from \"@paper-design/shaders-react\";\nimport type { SpiralProps } from \"@paper-design/shaders-react\";\nimport { createContext, useCallback, useContext, useState } from \"react\";\nimport type { CSSProperties, ReactNode } from \"react\";\n\nconst ShaderTimingContext = createContext(0);\n\ninterface ShaderLayoutProps {\n  fps?: number;\n  durationInFrames?: number;\n  width?: number;\n  height?: number;\n  className?: string;\n  children: ReactNode;\n}\n\nconst ShaderLayout = ({\n  fps = 30,\n  durationInFrames = 150,\n  width = 1280,\n  height = 720,\n  className,\n  children,\n}: ShaderLayoutProps) => {\n  const { ref, ownCurrentTimeMs } = useTimingInfo();\n  const durationMs = (durationInFrames / fps) * 1000;\n  const containerStyle: CSSProperties = {\n    height,\n    overflow: \"hidden\",\n    position: \"relative\",\n    width,\n  };\n\n  return (\n    <Timegroup\n      ref={ref}\n      className={className}\n      duration={`${durationMs}ms`}\n      mode=\"fixed\"\n      style={containerStyle}\n    >\n      <ShaderTimingContext.Provider value={ownCurrentTimeMs}>\n        {children}\n      </ShaderTimingContext.Provider>\n    </Timegroup>\n  );\n};\n\nconst useShaderFrame = (speed = 1) => {\n  const ownCurrentTimeMs = useContext(ShaderTimingContext);\n  return ownCurrentTimeMs * speed;\n};\n\nconst ShaderCanvas = ({\n  className,\n  children,\n}: {\n  className?: string;\n  children: ReactNode;\n}) => {\n  const [ready, setReady] = useState(false);\n  const gate = useCallback((element: HTMLDivElement | null) => {\n    if (!element) {\n      return;\n    }\n    requestAnimationFrame(() => requestAnimationFrame(() => setReady(true)));\n  }, []);\n\n  return (\n    <div\n      ref={gate}\n      className={className}\n      style={{ inset: 0, position: \"absolute\" }}\n    >\n      {ready ? children : null}\n    </div>\n  );\n};\n\nexport type ShaderSpiralProps = Omit<SpiralProps, \"frame\" | \"ref\">;\n\nexport interface ShaderSpiralWithLayout extends ShaderSpiralProps {\n  fps?: number;\n  durationInFrames?: number;\n  width?: number;\n  height?: number;\n}\n\nconst ShaderSpiralScene = ({\n  speed = 1,\n  colorBack = \"#12121a\",\n  colorFront = \"#52527a\",\n  density = 1,\n  strokeWidth = 0.5,\n  softness = 0.2,\n  className,\n  width = 1280,\n  height = 720,\n  ...rest\n}: ShaderSpiralProps & { width?: number; height?: number }) => {\n  const frame = useShaderFrame(speed);\n\n  return (\n    <ShaderCanvas className={className}>\n      <Spiral\n        speed={0}\n        frame={frame}\n        colorBack={colorBack}\n        colorFront={colorFront}\n        density={density}\n        strokeWidth={strokeWidth}\n        softness={softness}\n        fit=\"cover\"\n        width={width}\n        height={height}\n        {...rest}\n      />\n    </ShaderCanvas>\n  );\n};\n\nexport const ShaderSpiral = ({\n  speed = 1,\n  fps = 30,\n  durationInFrames = 150,\n  width = 1280,\n  height = 720,\n  className,\n  ...rest\n}: ShaderSpiralWithLayout) => (\n  <ShaderLayout\n    className={className}\n    durationInFrames={durationInFrames}\n    fps={fps}\n    height={height}\n    width={width}\n  >\n    <ShaderSpiralScene\n      className={className}\n      height={height}\n      speed={speed}\n      width={width}\n      {...rest}\n    />\n  </ShaderLayout>\n);\n",
      "type": "registry:component",
      "target": "components/framecn/shader-spiral.tsx"
    }
  ],
  "type": "registry:component"
}