---
title: "Accordion"
description: "A disclosure whose opened/closed state is a pure function of the timeline."
---

<ComponentPreview hideCustomizer name="accordion" />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @framecn/accordion
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="accordion" title="components/ui/accordion.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Accordion } from "@/components/ui/accordion";
```

```tsx
<Accordion />
```

## API Reference

### Accordion

| Prop       | Type                    | Default    |
| ---------- | ----------------------- | ---------- |
| `state`    | `AccordionState`        | `"closed"` |
| `from`     | `AccordionState`        | `-`        |
| `duration` | `string`                | `-`        |
| `items`    | `AccordionItem[]`       | `[]`       |
| `theme`    | `Partial<FramecnTheme>` | `-`        |
