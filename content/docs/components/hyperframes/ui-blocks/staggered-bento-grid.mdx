---
title: "Staggered Bento Grid"
description: "Bento grid where cards cascade in with per-cell sequences."
---

<ComponentPreview name="staggered-bento-grid" hideCode />

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">Command</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @framecn/staggered-bento-grid
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="staggered-bento-grid"
  title="components/ui/staggered-bento-grid.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { StaggeredBentoGrid } from "@/components/framecn/staggered-bento-grid";
```

```tsx
<StaggeredBentoGrid />
```

## API Reference

### StaggeredBentoGrid

| Prop               | Type          | Default         |
| ------------------ | ------------- | --------------- |
| `items`            | `BentoItem[]` | `DEFAULT_ITEMS` |
| `staggerDelay`     | `number`      | `8`             |
| `columns`          | `number`      | `3`             |
| `background`       | `string`      | `"#0a0a0a"`     |
| `cardColor`        | `string`      | `"#1a1a1a"`     |
| `textColor`        | `string`      | `"white"`       |
| `speed`            | `number`      | `1`             |
| `fps`              | `number`      | `30`            |
| `durationInFrames` | `number`      | `90`            |
| `className`        | `string`      | `-`             |

## Notes

<Callout title="Per-cell cascade animation">
  Cards cascade in with staggered timing. Each cell has its own animation
  sequence for a dynamic entrance effect.
</Callout>
